# Snip bundle

This branch is generated output. Do not hand-edit it.

The release contains the Bun API server, built Angular application, and CLI
assembled by `scripts/build-bundle.mjs` on the `main` branch.